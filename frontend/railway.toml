[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.frontend"

[deploy]
restartPolicyType = "on_failure"

[variables]
NODE_ENV = "production"

# Build variables - these are available during docker build
[build.variables]
VITE_API_URL = "https://flipiq-backend-production-7d65.up.railway.app"
